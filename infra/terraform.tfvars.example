# Copy to terraform.tfvars and fill in. Do NOT commit the real file.
subscription_id = "00000000-0000-0000-0000-000000000000"
owner_alias     = "kbk"
location        = "polandcentral"

# Your current public IP (run: curl -s ifconfig.me) — used only to open the
# SQL firewall for the one-time schema seed.
my_ip_address = "203.0.113.10"
